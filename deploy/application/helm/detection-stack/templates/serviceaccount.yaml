{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "detection-stack.serviceAccountName" . }}
  labels:
    app.kubernetes.io/part-of: fedkube-detection
{{- end }}
