image:
  repository: hieunguyen595/fedkube-gnn
  tag: unset
  digest: sha256:df0d4f0173ed4bdde6b489f8a22573302c2d746464705678fcaf1763b1458582
releaseId: 96a84f4e9763e707d7a57de46e293ab0e417f0a1
deploymentStrategy: RollingUpdate
deploymentMaxSurge: 0
deploymentMaxUnavailable: 1
modelBundle:
  existingClaim: ""
  existingSecret: fedper-model-bundle-serving-8d59392ff1
inference:
  enabled: true
  resources:
    requests: {cpu: 10m, memory: 768Mi}
    limits: {cpu: "2", memory: 2Gi}
demoTarget:
  enabled: true
  observation:
    enabled: false
    sensorId: sensor-34-1
  resources:
    requests: {cpu: 1m, memory: 64Mi}
    limits: {cpu: 250m, memory: 256Mi}
zeek:
  enabled: true
  # Capture both directions of pod-originated demo connections while excluding
  # kubelet probes SNATed through the first reserved Pod-range address.
  captureFilter: tcp and not host 10.40.0.1 and ((src net 10.40.0.0/14 and dst port 8080) or (dst net 10.40.0.0/14 and src port 8080))
  sensorId: sensor-34-1
  resources:
    requests: {cpu: 5m, memory: 256Mi}
    limits: {cpu: 500m, memory: 512Mi}
  shipperResources:
    requests: {cpu: 1m, memory: 64Mi}
    limits: {cpu: 250m, memory: 128Mi}
demoConsole:
  enabled: true
  monitorBufferSize: 500
  resources:
    requests: {cpu: 1m, memory: 64Mi}
    limits: {cpu: 500m, memory: 256Mi}
gateway:
  enabled: true
  resources:
    requests: {cpu: 1m, memory: 32Mi}
    limits: {cpu: 250m, memory: 128Mi}
collector:
  enabled: true
  monitorBufferSize: 500
  window:
    durationSeconds: 60
    maxFlows: 50
    emitStrideFlows: 1
    allowedLatenessSeconds: 1
  alertPolicy:
    enabled: true
    confidenceThreshold: 0.85
    confidenceBoundaries: [0.0, 0.5, 0.7, 0.85, 0.95, 1.0]
    entropyBoundaries: [0.0, 0.25, 0.5, 1.0, 2.0]
    classSeverity:
      Attack: low
      C&C: high
      C&C-HeartBeat: medium
      DDoS: high
      Okiru: high
      PartOfAHorizontalPortScan: medium
    entityHashSecretName: fedkube-detection-detection-stack-entity-hash
  resources:
    requests: {cpu: 5m, memory: 128Mi}
    limits: {cpu: "1", memory: 512Mi}
alertRouter:
  enabled: true
  resources:
    requests: {cpu: 1m, memory: 64Mi}
    limits: {cpu: 500m, memory: 256Mi}
evaluation:
  enabled: false
  artifactsClaim:
    retain: true
ingress:
  enabled: false
elasticsearch:
  enabled: true
  endpoint: https://fedper-detection-es-http:9200
  index: fedper-detections
  username: elastic
  credentialsSecretName: fedper-detection-es-elastic-user
  credentialsSecretKey: elastic
  caSecretName: fedper-detection-es-http-certs-public
elasticStack:
  enabled: true
  name: fedper-detection
  version: 9.2.3
  kibanaCount: 1
  kibanaNodeOptions: --max-old-space-size=512
  bootstrapRevision: v4-dashboard
  storageClassName: standard-rwo
  storageSize: 10Gi
  elasticsearchResources:
    requests: {cpu: 10m, memory: 2Gi}
    limits: {cpu: "1", memory: 2Gi}
  kibanaResources:
    requests: {cpu: 25m, memory: 768Mi}
    limits: {cpu: 500m, memory: 1Gi}
externalSecrets:
  enabled: true
